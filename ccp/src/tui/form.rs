use ratatui::{
    layout::Rect,
    Frame,
};

use crate::config::profile::Profile;

use super::app::App;

/// 渲染表单——由后续任务完整实现
pub fn render_form(f: &mut Frame, area: Rect, app: &mut App) {
    // Stub — form rendering implemented in a later task
}